language: node_js
node_js:
  - 8
cache:
  directories:
    - node_modules
script:
  - npm run test:unit
env:
  - CI=true

before_deploy:
  - npm install netlify-cli -g
  - npm run build

deploy:
  provider: script
  script: netlify deploy -a $NETLIFY_AUTH_TOKEN -s $NETLIFY_SITE_ID -p ./build